specVersion: 1.0.0
version: 0.0.1
name: drop-points
description: Drop points
runner:
  node:
    name: '@subql/node-cosmos'
    version: '>=3.0.0'
  query:
    name: '@subql/query'
    version: '*'
schema:
  file: ./schema.graphql
network:
  chainId: secret-4
  endpoint:
    # - https://rpc-secret.01node.com
    # # - https://scrt.public-rpc.com
    - https://rpc.mainnet.secretsaturn.net
dataSources:
  - kind: cosmos/Runtime
    startBlock: 14343000
    mapping:
      file: ./dist/index.js
      handlers:
        - handler: handleCoinSecret
          kind: cosmos/TransactionHandler
